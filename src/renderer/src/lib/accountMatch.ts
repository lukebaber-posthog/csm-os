/*
 * Account-name matching, used by the to-do composer to link a to-do from its
 * words and by the MCP server to accept a name where the board wants an org id.
 */
export * from '../../../core/accountMatch'
